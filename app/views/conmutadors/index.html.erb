<div class="row">
  <div class="col-md-6">
    <h1>Bienvenido a Reparify</h1>
    <%= link_to 'Testeo', tests_path , :class => 'btn btn-primary'%>
    <%= link_to 'ADMIN', admin_path , :class => 'btn btn-info'%>
  </div>
</div>















<!-- <p id="notice"><%= notice %></p>

<h1>Conmutadors</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @conmutadors.each do |conmutador| %>
      <tr>
        <td><%= link_to 'Show', conmutador %></td>
        <td><%= link_to 'Edit', edit_conmutador_path(conmutador) %></td>
        <td><%= link_to 'Destroy', conmutador, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Conmutador', new_conmutador_path %>
 -->